//  Filter, map, reduce

const numeros = [5, 50, 10, 50, 8, 2, 65, 11];

const numerosFiltrados = numeros.filter(valor => valor > 10); // Passando uma função callback no Filter
console.log(numerosFiltrados)

